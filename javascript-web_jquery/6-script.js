// Actualiza el texto de la etiqueta de encabezado con un nuevo encabezado cuando se hace clic en la etiqueta
// se hace clic en la etiqueta DIV#update_header

$('div#update_header').click(function () {
    $('header').text('New Header!!!');
});